image: "python:3.8"

before_script:
  - python3 --version

stages:
  - test

static-analysis:
  stage: test
  script:
    - python3 -m pip install -r requirements.txt
    - pylint release_notes_generator.py
  tags:
    - python