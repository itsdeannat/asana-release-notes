image: python:latest

stages:
  - test

before_script:
  - python3 -m pip install -r requirements.txt

static-analysis:
  stage: test
  script:
    - echo "Running pylint"
    - pylint release-notes-generator.py
  tags:
    - python